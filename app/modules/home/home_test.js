'use strict';

describe('chapters.home module', function() {

	beforeEach(module('chapters.home'));

	describe('home controller', function() {

		it('should ....', inject(function($controller) {
			//spec body
			var homeCtrl = $controller('HomeCtrl');
			expect(homeCtrl).toBeDefined();
		}));

	});
});
